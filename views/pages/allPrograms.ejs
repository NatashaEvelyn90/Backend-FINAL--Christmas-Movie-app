<%- include('../partials/header') %>

<main class="main" id="main">
    <div class="container">
        <div class="row">
            <% data.forEach(program => { %>
                <figure>
                    <img src="<%= program.img_url %>" alt="">
                </figure>
                
            <% }) %>
        </div>
    </div>
</main>

<%- include('../partials/footer') %>